# LA Clipper Intel — Website Backlog

_Maintained by the weekly Website & SEO Manager task. Replaced in full each run; changelog at the bottom keeps the last 5 runs._

> **Note on location:** this file is meant to live at the root of `LA-Clipper-Intel/Clipper-Intel-Productions`, but this session's GitHub access is scoped to `laclipperintel-website` only (confirmed via the GitHub MCP tools — `Clipper-Intel-Productions` exists but returns "not configured for this session"). Until that scope is widened, this backlog lives here instead.

## Fixed this run (2026-09-26)

- [Compress hero images and add WebP variants for banner/logo](https://github.com/LA-Clipper-Intel/laclipperintel-website/commit/bf095ec)
- [Add full favicon set, web app manifest, and WebP picture wiring](https://github.com/LA-Clipper-Intel/laclipperintel-website/commit/51e98e6)
- Opened [PR #1](https://github.com/LA-Clipper-Intel/laclipperintel-website/pull/1) with both commits — **not yet merged to `main`**, see "Needs Kyle" below.

Summary: this session had `pip`/PyPI egress that prior runs didn't, so installed Pillow and finally cleared two long-standing P1/P2 items. Recompressed `banner.jpg` (198KB→143KB) and `logo.jpg` (122KB→29KB, also downsized 1024px→512px since it only ever renders as a 38px avatar or a schema.org logo reference), added `banner.webp`/`logo.webp`, and wired both images into `<picture>` elements with WebP-first/JPEG-fallback. Generated a full favicon set (`favicon.ico` at 16/32/48, `favicon-16.png`, `favicon-32.png`, `icon-192.png`, `icon-512.png`) plus `site.webmanifest`, all palette-quantized for size; re-optimized `apple-touch-icon.png` (57KB→24KB) the same way. Bumped `.icon-btn` tap targets 38px→44px for touch-target accessibility. Verified locally with `http-server` + a headless Playwright pass at 375px/1280px: no horizontal scroll, no new console errors, screenshots confirm no visual regression. Re-verified this run's breaking-news content (Kawhi-to-Toronto trade, Ballmer suspension/fine, John Gibson interim governor, Hachimura/Beal/Strus signings) against fresh web search — all still accurate, nothing new to add to the ticker since last run.

**This session could not fetch the live `laclipperintel.com` domain directly** — the network egress proxy returns a hard policy denial (403) for that host, unlike prior runs. Deploy verification instead relied on confirming the `pages build and deployment` GitHub Action is green on `main`'s current tip and reviewing the repo source directly (which is what Pages serves as a static site, so this is a reliable proxy for "what's live"). Flagging in case this is an unintended new restriction — see "Needs Kyle."

## Do next — ready for Claude Code

- **P0 — Merge PR #1 to `main` so this run's fixes actually go live.** This session was given a "develop on `claude/pensive-johnson-xttz6s`, never push to a different branch without explicit permission" instruction that wasn't present (or wasn't followed) in prior runs — all of which committed straight to `main`. Out of caution this run opened a PR instead of pushing/merging to `main` directly. If future runs get the same branch restriction, the routine needs either (a) permission to merge its own PRs when there's no CI and no other reviewer, or (b) a standing human/reviewer step — right now nothing merges these PRs, so fixes pile up unreleased.
- **P1 — Real video embeds.** `#reelrow` (Quick Hits) and `#ylist` (Deep Dive) still show honest but generic placeholder tiles, not real videos — carried over again because vidIQ credits were still unavailable/unused this run. Once credits are available, pull the real list (`vidiq_channel_videos` for `@LAClipperIntel`, both `short` and `long` formats) and swap in real thumbnails/titles/durations, embedding with `youtube-nocookie.com` + lazy loading per the standing site rules.
- **P1 — Re-verify GM Meter cap figures precisely.** The $6.4M/$17M TPE/$1.0M MLE/$5.4M BAE numbers in `#gm` are still labeled "Cap sheet as of Sept 25, 2026" as a stopgap. This run's web search on the Clippers' exact remaining-exception figures didn't turn up a source precise enough to confirm or correct the exact numbers (only general MLE-tier figures, e.g. taxpayer MLE ≈ $6.06M league-wide). Needs a pass with a real cap-tracking source (Spotrac/Bobby Marks/Yossi Gozlan) to either confirm exact figures or update them, then refresh the date label.
- **P2 — Nicer OG image.** `assets/og-image.jpg` is still a screenshot composited from the banner/logo — functional and correctly sized (1200×630, 87KB), but a purpose-designed card (e.g. via Canva, now that this session has Canva access) would look sharper on social.

## Needs Kyle

- **PR #1 needs a merge decision.** https://github.com/LA-Clipper-Intel/laclipperintel-website/pull/1 — image compression, favicon set, manifest, WebP picture tags, tap-target fix. Clean/mergeable, no CI configured on this repo. Please merge (or tell the routine it's OK to merge its own clean PRs going forward) so these fixes actually deploy.
- **Live-domain egress blocked for this session.** `laclipperintel.com` returned a hard 403 policy denial from this session's network proxy (confirmed via the proxy status endpoint), so this run could not directly re-fetch the live page the way prior runs did. Worth checking whether this is an intended new restriction on this environment — if not, future runs lose the ability to catch things that only show up live (redirect behavior, CDN caching, actual rendered fonts/images) rather than in the repo source.
- **Memory tool still unavailable this session.** No `mcp__memory__*` tool and no "memory" MCP connector were present (checked via `ListConnectors`), so Step 5 (updating `/areas/clipper-intel.md` with a WEBSITE STATUS section) could not be completed again this run, and this run also could not read the Analytics manager's "CONTENT PERFORMANCE GUIDANCE" to confirm what content pillar the site should lead with. This has now been flagged three runs in a row — worth checking the task's connector configuration.
- **Google Search Console.** Still outstanding — verify the domain and submit `https://laclipperintel.com/sitemap.xml`.
- **Real email-alert list.** Still outstanding — no ESP connected, so the site honestly says "no email list yet" instead of faking capture.
- **vidIQ credits.** Still needed to unblock the real-video-embeds item above.
- **GitHub scope.** Still only covers `laclipperintel-website`; `Clipper-Intel-Productions` (private) and `Clipper-Intel-Media-Staging` (public) remain out of reach.
- **"Clippers Beat Monitor" task.** Per standing instructions this was reported to be updating an old claude.ai artifact copy of the site rather than this live GitHub Pages repo — still unconfirmed/unresolved as far as this session can tell.

## Changelog (last 5 runs)

- **2026-09-26** — Installed Pillow (PyPI egress worked this run) and cleared two backlog items: compressed banner/logo images + added WebP picture wiring, and built a full favicon set + web app manifest. Opened PR #1 rather than pushing to `main` directly, due to a new branch restriction on this session — not yet merged. Live-domain fetch was blocked by network policy this run (worked around via GitHub Actions deploy-status + repo source review). Re-verified breaking news is still accurate; no ticker changes needed.
- **2026-09-25** — First backlog write (no prior WEBSITE_BACKLOG.md found in this repo). Shipped SEO essentials (meta/OG/Twitter/JSON-LD/robots/sitemap/404/favicon/OG image) and fixed stale/fabricated content (Broome, Trey Murphy rumor, fake view counts/video specifics, fake email capture). See prior run details in git history (commits c2265ec, c416fb7).
